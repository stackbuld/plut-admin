# Copy to .env.local (gitignored) and adjust for local development.
# If unset, src/api/client.ts falls back to the production API — safe to leave unset in any
# environment that hasn't been configured yet.

# VITE_API_BASE_URL=http://localhost:9090
